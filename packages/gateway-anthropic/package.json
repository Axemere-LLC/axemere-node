{
  "name": "@axemere/gateway-anthropic",
  "version": "0.1.7",
  "description": "Axemere AI Gateway SDK — Anthropic drop-in wrapper",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Axemere-LLC/axemere-node.git",
    "directory": "packages/gateway-anthropic"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@axemere/gateway": "workspace:*",
    "@anthropic-ai/sdk": "^0.36.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "^.+\\.tsx?$": ["ts-jest", { "useESM": false }]
    }
  }
}
