{
  "name": "@axemere/gateway-langchain",
  "version": "0.1.9",
  "description": "Axemere AI Gateway SDK — LangChain BaseChatModel integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Axemere-LLC/axemere-node.git",
    "directory": "packages/gateway-langchain"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "peerDependencies": {
    "@langchain/core": ">=0.3.0"
  },
  "dependencies": {
    "@axemere/gateway": "workspace:*",
    "@axemere/gateway-openai": "workspace:*",
    "@axemere/gateway-anthropic": "workspace:*"
  },
  "devDependencies": {
    "@langchain/core": "^0.3.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": {
      "^.+\\.tsx?$": ["ts-jest", { "useESM": false }]
    }
  }
}
